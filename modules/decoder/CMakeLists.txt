cmake_minimum_required (VERSION 3.6)

if (TARGET_WEBOS)
    add_subdirectory(smp)
    add_subdirectory(cgl)
elseif (TARGET_RASPI)
    add_subdirectory(ffmpeg)
else()
    add_subdirectory(ffmpeg)
endif()